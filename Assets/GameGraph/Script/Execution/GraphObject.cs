namespace GameGraph
{
    public class GraphObject
    {
        // TODO This is used for the graph representation
        
        // @see MaterialGraphEditWindow line 518
        //var textGraph = File.ReadAllText(path, Encoding.UTF8);
        //graphObject = CreateInstance<GraphObject>();
        //graphObject.hideFlags = HideFlags.HideAndDontSave;
        //graphObject.graph = JsonUtility.FromJson<GraphData>(textGraph);
        //graphObject.graph.assetGuid = assetGuid;
        //graphObject.graph.isSubGraph = isSubGraph;
        //graphObject.graph.messageManager = messageManager;
        //graphObject.graph.OnEnable();
        //graphObject.graph.ValidateGraph();
        
        
        // @see GraphObject
        //public GraphData graph
        //{
        //    get { return m_Graph; }
        //    set
        //    {
        //        if (m_Graph != null)
        //            m_Graph.owner = null;
        //        m_Graph = value;
        //        if (m_Graph != null)
        //            m_Graph.owner = this;
        //    }
        //}
        //
        //public bool isDirty
        //{
        //    get { return m_IsDirty; }
        //    set { m_IsDirty = value; }
        //}
    }
}
